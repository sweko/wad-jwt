{
    "name": "dev-vs-ciso-jwt-playground",
    "version": "1.0.0",
    "description": "Generate and inspect JWTs, and see the difference between decoding and verifying one",
    "main": "dist/app.js",
    "scripts": {
        "render": "tsc && node dist/app.js",
        "start": "node dist/app.js",
        "dev": "nodemon src/app.ts",
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "express": "^4.17.1"
    },
    "devDependencies": {
        "@types/express": "^4.17.13",
        "@types/node": "^16.11.7",
        "nodemon": "^3.1.4",
        "ts-node": "^10.4.0",
        "typescript": "^4.5.2"
    }
}
